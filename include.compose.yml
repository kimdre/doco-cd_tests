services:
  included:
    image: nginx:latest
    restart: unless-stopped
    labels:
      included: 3
    configs:
      - include.conf
  other:
    image: nginx:latest
    restart: unless-stopped
    labels:
      included: 3
      cd.doco.deployment.recreate.ignore: >-
        {
          configs: [app, nginx],
          secrets: [db],
          bindMounts: [/etc/caddy]
        }

configs:
  include.conf:
    file: include.conf
