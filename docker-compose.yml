services:
  test:
    container_name: test
    image: nginx
    restart: unless-stopped
    environment:
      TEST_PASSWORD: hello!
      TZ: Europe/Berlin
      HELLO: World!
      Foo: bar
    labels:
      test1: inject.${TEST_PASSWORD}
      test2: ignore.$${TEST_PASSWORD}
      a: d
    env_file: ./foo.env
