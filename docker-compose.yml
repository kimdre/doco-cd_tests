services:
  test-app:
      scale: 1
      image: nginx
      restart: unless-stopped
      environment:
        TZ: Europe/Berlin
        HELLO: World
      ports:
        - "81:80"
      networks:
        - web
      volumes:
        - web:/test

networks:
  web:

volumes:
  web:
