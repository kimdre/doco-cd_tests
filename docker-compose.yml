services:
  test:
      scale: 1
      container_name: test
      image: nginx
      restart: unless-stopped
      environment:
        TZ: Europe/Berlin
        HELLO: World
      ports:
        - "8080:80"
      volumes:
        - ./html/usr/share/nginx/html
