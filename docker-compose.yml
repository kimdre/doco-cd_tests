services:
  test:
    scale: 1
    container_name: test
    image: nginx
    restart: unless-stopped
    environment:
      TZ: Europe/Berlin
      DESTROY: true
    ports:
      - "4321:80"
    volumes:
      - ./html:/usr/share/nginx/html
