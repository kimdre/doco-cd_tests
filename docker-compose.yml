services:
  test:
    container_name: test
    image: nginx
    restart: unless-stopped
    environment:
      TZ: Europe/Berlin
      HELLO: World!
      Foo: bar
    labels:
      test: injected.${TEST_PASSWORD}
      ignore: ignore.\\${TEST_PASSWORD}
    env_file: ./foo.env
