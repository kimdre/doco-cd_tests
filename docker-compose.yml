services:
  test:
    container_name: test
    image: nginx
    restart: unless-stopped
    environment
      TZ: Europe/Berlin
      HELLO: World!
      Foo: bar
    labels:
      test1: inject.${TEST_PASSWORD}
      test2: ignore.$${TEST_PASSWORD}
    env_file: ./foo.env
